import React, { useEffect, useRef } from 'react'

import * as sc from './styled'

export default function Background_Wrapper() {
  return (
    <sc.MainWrapper>
      <sc.Background/>
    </sc.MainWrapper>
  )
}